import React from "react";
import { SafeAreaView } from "react-native";

const AppNavigator = () => {
    return (
        <SafeAreaView>
            
        </SafeAreaView>
    );
}

export default AppNavigator;